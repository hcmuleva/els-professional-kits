server {
    listen 80;

    location / {
        # 'alias' maps the URL subpath to the correct file path inside the container.
        alias /usr/share/nginx/html/;

        # This handles both client-side routing and serves the index.html fallback.
        try_files $uri $uri/ /index.html;
    }
}
