import HomeController from "../components/user/home/HomeController";

const HomePage = () => {
  // const {user} = useAuth(AuthContext)
  // Sample subscription categories data
  // if(user?.userstatus !=="APPROVED") return <PendingStatus/>
  return (
    <div style={{}}>
      <HomeController />
    </div>
  );
};

export default HomePage;
