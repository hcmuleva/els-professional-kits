apiVersion: v1
kind: Service
metadata:
  name: hello-world
spec:
  selector: { app.kubernetes.io/name: hello-world }
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.containerPort }}
